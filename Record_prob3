package housing;

import java.util.*;

public class Record_prob3 
{
	public static void main(String args[])
	{
		Map<String, Collection<String>> hmap = new HashMap<String, Collection<String>>();
		if(menu() == 1)
			inputName();
		else if(menu() == 2)
		{
			
		}
	}
	
	public static int menu()
	{
		Scanner input = new Scanner(System.in);
		System.out.println("Press 1 to input a name:");
		System.out.println("Press 2 to check if a person is still using the service.");
		int ans = input.nextInt();
		return ans;
	}
	
	public static void inputName()
	{
		Scanner input1 = new Scanner(System.in);
		System.out.println("Enter a name:");
		String name = input1.nextLine();
	}
}
